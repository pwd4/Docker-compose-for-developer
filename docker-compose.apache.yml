{
    "version": "2",
    "volumes": {
        "postgres_data": {
            "driver": "local"
        }
    },
    "services": {
        "postgres": {
            "image": "postgres",
            "volumes": [
                "postgres_data:/var/lib/postgresql/data"
            ]
        },
        "apache": {
            "build": {
                "context": ".",
                "dockerfile": "Dockerfile-apache+postgres"
            },
            "volumes": [
                "./:/var/www/html"
            ],
            "ports": [
                "80:80"
            ],
            "links": [
                "postgres"
            ],
            "depends_on": [
                "postgres"
            ]
        }
    }
}
