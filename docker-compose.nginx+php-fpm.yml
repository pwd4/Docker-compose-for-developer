{
    "version": "2",
    "volumes": {
        "postgres_data": {
            "driver": "local"
        }
    },
    "services": {
        "postgres": {
            "image": "postgres",
            "volumes": [
                "postgres_data:/var/lib/postgresql/data"
            ]
        },
        "php-fpm": {
            "build": {
                "context": ".",
                "dockerfile": "Dockerfile-php-fpm+postgres"
            },
            "volumes": [
                "./:/var/www/html"
            ],
            "links": [
                "postgres"
            ],
            "depends_on": [
                "postgres"
            ]
        },
        "nginx": {
            "image": "nginx:alpine",
            "volumes": [
                "./:/var/www",
                "./nginx.php-fpm.conf:/etc/nginx/nginx.conf:ro"
            ],
            "links": [
                "php-fpm"
            ],
            "ports": [
                "80:80"
            ],
            "depends_on": [
                "php-fpm"
            ]
        }
    }
}
